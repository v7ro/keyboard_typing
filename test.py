#!/usr/bin/env python3
"""
Тесты для модуля KeyboardAnalyzer из main.py
"""
import inspect
import pytest
import sys
import os
import tempfile

# Добавляем текущую директорию в путь для импорта
current_dir = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, current_dir)

from main import KeyboardAnalyzer, get_common_chars

class TestKeyboardAnalyzerLayouts:
    """
    Набор юнит‑тестов для проверки корректной инициализации раскладок
    в классе ``KeyboardAnalyzer``.

    В тестах проверяется:
    - корректность установки атрибута ``layout`` при создании анализатора,
    - наличие ожидаемых кодов клавиш в карте клавиатуры (``keyboard_map``),
    - поддержка всех предопределённых раскладок:
      * ``ytsuken`` — стандартная,
      * ``vyzov`` — Вызов,
      * ``zubachev`` — Зубачев,
      * ``skoropis`` — Скоропись,
      * ``rusfon`` — Русфон,
      * ``diktor`` — Диктор,
      * ``ant`` — Ант.
    """
    def test_ytsuken_layout(self):
        """
        Проверяет корректную инициализацию раскладки ``ytsuken`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку установки атрибута ``layout`` в значение ``'ytsuken'``,
        - проверку наличия ожидаемого кода клавиши (``2``) в карте клавиатуры ``keyboard_map``.

        Используется для гарантии корректной загрузки стандартной раскладки
        и формирования базовой карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ytsuken')
        assert analyzer.layout == 'ytsuken'
        assert 2 in analyzer.keyboard_map

    def test_vyzov_layout(self):
        """
        Проверяет корректную инициализацию раскладки ``vyzov`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку установки атрибута ``layout`` в значение ``'vyzov'``,
        - проверку наличия ожидаемого кода клавиши (``16``) в карте клавиатуры ``keyboard_map``.

        Используется для гарантии корректной загрузки раскладки «Вызов» и формирования
        базовой карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('vyzov')
        assert analyzer.layout == 'vyzov'
        assert 16 in analyzer.keyboard_map

    def test_zubachev_layout(self):
        """
        Проверяет корректную инициализацию раскладки ``zubachev`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку установки атрибута ``layout`` в значение ``'zubachev'``,
        - проверку наличия ожидаемого кода клавиши (``30``) в карте клавиатуры ``keyboard_map``.

        Используется для гарантии корректной загрузки раскладки «Зубачев» и формирования
        базовой карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('zubachev')
        assert analyzer.layout == 'zubachev'
        assert 30 in analyzer.keyboard_map

    def test_skoropis_layout(self):
        """
        Проверяет корректную инициализацию раскладки ``skoropis`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку установки атрибута ``layout`` в значение ``'skoropis'``,
        - проверку наличия ожидаемого кода клавиши (``41``) в карте клавиатуры ``keyboard_map``.

        Используется для гарантии корректной загрузки раскладки «Скоропись» и формирования
        базовой карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('skoropis')
        assert analyzer.layout == 'skoropis'
        assert 41 in analyzer.keyboard_map

    def test_rusfon_layout(self):
        """
        Проверяет корректную инициализацию раскладки ``rusfon`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку установки атрибута ``layout`` в значение ``'rusfon'``,
        - проверку наличия ожидаемого кода клавиши (``42``) в карте клавиатуры ``keyboard_map``.

        Используется для гарантии корректной загрузки раскладки «Русфон» и формирования
        базовой карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('rusfon')
        assert analyzer.layout == 'rusfon'
        assert 42 in analyzer.keyboard_map

    def test_diktor_layout(self):
        """
        Проверяет корректную инициализацию раскладки ``diktor`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку установки атрибута ``layout`` в значение ``'diktor'``,
        - проверку наличия ожидаемого кода клавиши (``43``) в карте клавиатуры ``keyboard_map``.

        Используется для гарантии корректной загрузки раскладки «Диктор» и формирования
        базовой карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('diktor')
        assert analyzer.layout == 'diktor'
        assert 43 in analyzer.keyboard_map

    def test_ant_layout(self):
        """
        Проверяет корректную инициализацию раскладки ``ant`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку установки атрибута ``layout`` в значение ``'ant'``,
        - проверку наличия ожидаемого кода клавиши (``57``) в карте клавиатуры ``keyboard_map``.

        Используется для гарантии корректной загрузки раскладки «Ант» и формирования
        базовой карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ant')
        assert analyzer.layout == 'ant'
        assert 57 in analyzer.keyboard_map

class TestYtsukenLayout:
    """
    Набор юнит‑тестов для проверки корректной инициализации раскладки ``ytsuken``
    в классе ``KeyboardAnalyzer``.

    Включает:
    - тестирование основной карты клавиш (``keys``),
    - проверку генерации заглавных букв (``caps_keys``),
    - проверку символов, доступных через Shift (``shift_keys``),
    - проверку позиций пальцев в домашнем ряду (``home_positions``).

    Используется для гарантии корректной загрузки раскладки «Ytsuken» и формирования
    всех вспомогательных структур, необходимых для анализа текста.
    """
    def test_keys_mapping(self):
        """
        Проверяет корректность основной карты клавиш (``keys``) для раскладки ``ytsuken``
        в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия символов их ожидаемым кодам клавиш и пальцам:
          * ``'й'`` → ``(16, 'left_pinky')``,
          * ``'н'`` → ``(21, 'right_index')``,
          * ``'ю'`` → ``(52, 'right_ring')``,
          * пробел (``' '``) → ``(57, 'right_thumb')``.

        Используется для гарантии корректной загрузки раскладки «Ytsuken» и формирования
        основной карты клавиш.
        
        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ytsuken')
        keys = analyzer.keys

        # Проверим несколько ключей
        assert keys['й'] == (16, 'left_pinky')
        assert keys['н'] == (21, 'right_index')
        assert keys['ю'] == (52, 'right_ring')
        assert keys[' '] == (57, 'right_thumb')

    def test_caps_keys_generation(self):
        """
        Проверяет корректность генерации словаря заглавных букв (``caps_keys``)
        для раскладки ``ytsuken`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия заглавных символов их ожидаемым кодам клавиш и пальцам:
          * ``'Й'`` → ``(16, 'left_pinky')``,
          * ``'Н'`` → ``(21, 'right_index')``,
        - проверку отсутствия пробела (``' '``) в словаре ``caps_keys``,
          так как он не должен иметь заглавного варианта.

        Используется для гарантии корректной генерации заглавных букв при инициализации
        раскладки «Ytsuken».
        
        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ytsuken')
        caps = analyzer.caps_keys

        # Проверим заглавные буквы
        assert caps['Й'] == (16, 'left_pinky')
        assert caps['Н'] == (21, 'right_index')
        assert ' ' not in caps  # пробел не должен быть в caps_keys

    def test_shift_keys_mapping(self):
        """
        Проверяет корректность словаря символов, вводимых через клавишу Shift (``shift_keys``)
        для раскладки ``ytsuken`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия специальных символов их ожидаемым кодам клавиш и пальцам:
          * ``'!'`` → ``(2, 'left_pinky')``,
          * ``'?'`` → ``(8, 'right_ring')``,
          * ``'+'`` → ``(13, 'right_pinky')``.

        Используется для гарантии корректной генерации карты клавиш с учётом
        символов, доступных через Shift, при инициализации раскладки «Ytsuken».
        
        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ytsuken')
        shift = analyzer.shift_keys

        # Проверим несколько символов с Shift
        assert shift['!'] == (2, 'left_pinky')
        assert shift['?'] == (8, 'right_ring')
        assert shift['+'] == (13, 'right_pinky')

    def test_home_positions(self):
        """
        Проверяет корректность словаря домашних позиций пальцев (``home_positions``)
        для раскладки ``ytsuken`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку позиции левого указательного пальца (``left_index``) — ожидается код клавиши ``33``,
        - проверку позиции правого указательного пальца (``right_index``) — ожидается код клавиши ``36``,
        - проверку позиции правого большого пальца (``right_thumb``) — ожидается код клавиши ``57``.

        Используется для гарантии корректной инициализации домашнего ряда раскладки «Ytsuken».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ytsuken')
        home = analyzer.home_positions

        # Проверим домашние позиции
        assert home['left_index'] == 33
        assert home['right_index'] == 36
        assert home['right_thumb'] == 57

class TestVyzovLayout:
    """
    Набор юнит‑тестов для проверки корректной инициализации раскладки ``vyzov``
    в классе ``KeyboardAnalyzer``.

    Включает:
    - тестирование основной карты клавиш (``keys``),
    - проверку генерации заглавных букв (``caps_keys``),
    - проверку символов, доступных через Shift (``shift_keys``),
    - проверку позиций пальцев в домашнем ряду (``home_positions``).

    Используется для гарантии корректной загрузки раскладки «Вызов» и формирования
    всех вспомогательных структур, необходимых для анализа текста.
    """
    def test_keys_mapping(self):
        """
        Проверяет корректность основной карты клавиш (``keys``) для раскладки ``vyzov``
        в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку наличия базовых символов (например, ``'а'``, ``'о'``) в словаре ``keys``,
        - проверку, что значения для символов представлены кортежами из двух элементов
          (код клавиши и палец),
        - проверку корректного отображения пробела (``' '``) на ожидаемую позицию
          ``(57, 'right_thumb')``.

        Используется для гарантии корректной загрузки раскладки «Вызов» и формирования
        основной карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('vyzov')
        keys = analyzer.keys

        # Проверим несколько ключей (замени на реальные из _init_vyzov_layout)
        assert 'а' in keys
        assert isinstance(keys['а'], tuple)
        assert len(keys['а']) == 2

        assert 'о' in keys
        assert isinstance(keys['о'], tuple)

        assert ' ' in keys
        assert keys[' '] == (57, 'right_thumb')

    def test_caps_keys_generation(self):
        """
        Проверяет корректность генерации словаря заглавных букв (``caps_keys``)
        для раскладки ``vyzov`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку наличия заглавных символов (например, ``'А'``) в словаре ``caps_keys``,
        - проверку, что значения для заглавных букв представлены кортежами (код клавиши и палец),
        - проверку отсутствия пробела (``' '``) в словаре ``caps_keys``,
          так как он не должен иметь заглавного варианта.

        Используется для гарантии корректной генерации заглавных букв при инициализации
        раскладки «Вызов».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('vyzov')
        caps = analyzer.caps_keys

        # Проверим заглавные буквы
        assert 'А' in caps
        assert isinstance(caps['А'], tuple)
        assert ' ' not in caps

    def test_shift_keys_mapping(self):
        """
        Проверяет корректность словаря символов, вводимых через клавишу Shift (``shift_keys``)
        для раскладки ``vyzov`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку наличия специальных символов (например, ``'!'``, ``'+'``) в словаре ``shift_keys``,
        - проверку, что значения для этих символов представлены кортежами (код клавиши и палец).

        Используется для гарантии корректной генерации карты клавиш с учётом
        символов, доступных через Shift, при инициализации раскладки «Вызов».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('vyzov')
        shift = analyzer.shift_keys

        # Проверим несколько символов с Shift
        assert '!' in shift
        assert isinstance(shift['!'], tuple)

        assert '+' in shift
        assert isinstance(shift['+'], tuple)

    def test_home_positions(self):
        """
        Проверяет корректность словаря домашних позиций пальцев (``home_positions``)
        для раскладки ``vyzov`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку позиции левого указательного пальца (``left_index``) — ожидается код клавиши ``33``,
        - проверку позиции правого указательного пальца (``right_index``) — ожидается код клавиши ``36``,
        - проверку позиции правого большого пальца (``right_thumb``) — ожидается код клавиши ``57``.

        Используется для гарантии корректной инициализации домашнего ряда раскладки «Вызов».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('vyzov')
        home = analyzer.home_positions

        # Проверим домашние позиции
        assert home['left_index'] == 33
        assert home['right_index'] == 36
        assert home['right_thumb'] == 57

class TestZubachevLayout:
    """
    Набор юнит‑тестов для проверки корректной инициализации раскладки ``zubachev``
    в классе ``KeyboardAnalyzer``.

    Включает:
    - тестирование основной карты клавиш (``keys``): проверка соответствия символов их кодам клавиш и пальцам,
    - проверку генерации заглавных букв (``caps_keys``): наличие корректных заглавных символов и отсутствие пробела,
    - проверку символов, вводимых через Shift (``shift_keys``): соответствие специальных символов ожидаемым позициям,
    - проверку позиций пальцев в домашнем ряду (``home_positions``): правильность кодов клавиш для указательных пальцев и большого пальца.

    Используется для гарантии корректной загрузки раскладки «Зубачёв» и формирования
    всех вспомогательных структур, необходимых для анализа текста и эргономики набора.
    """
    def test_keys_mapping(self):
        """
        Проверяет корректность основной карты клавиш (``keys``) для раскладки ``zubachev``
        в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия конкретных символов их ожидаемым кодам клавиш и пальцам:
          * ``'ф'`` → ``(16, 'left_pinky')``,
          * ``'я'`` → ``(19, 'left_index')``,
          * ``'р'`` → ``(23, 'right_index')``,
          * ``'ч'`` → ``(53, 'right_pinky')``,
          * пробел (``' '``) → ``(57, 'right_thumb')``.

        Используется для гарантии корректной загрузки раскладки «Зубачёв» и формирования
        основной карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('zubachev')
        keys = analyzer.keys

        # Проверим несколько конкретных символов
        assert keys['ф'] == (16, 'left_pinky')
        assert keys['я'] == (19, 'left_index')
        assert keys['р'] == (23, 'right_index')
        assert keys['ч'] == (53, 'right_pinky')
        assert keys[' '] == (57, 'right_thumb')

    def test_caps_keys_generation(self):
        """
        Проверяет корректность генерации словаря заглавных букв (``caps_keys``)
        для раскладки ``zubachev`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия заглавных символов их ожидаемым кодам клавиш и пальцам:
          * ``'Ф'`` → ``(16, 'left_pinky')``,
          * ``'Я'`` → ``(19, 'left_index')``,
          * ``'Ч'`` → ``(53, 'right_pinky')``,
        - проверку отсутствия пробела (``' '``) в словаре ``caps_keys``,
          так как он не должен иметь заглавного варианта.

        Используется для гарантии корректной генерации заглавных букв при инициализации
        раскладки «Зубачёв».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('zubachev')
        caps = analyzer.caps_keys

        # Проверим заглавные буквы
        assert caps['Ф'] == (16, 'left_pinky')
        assert caps['Я'] == (19, 'left_index')
        assert caps['Ч'] == (53, 'right_pinky')
        assert ' ' not in caps

    def test_shift_keys_mapping(self):
        """
        Проверяет корректность словаря символов, вводимых через клавишу Shift (``shift_keys``)
        для раскладки ``zubachev`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия специальных символов их ожидаемым кодам клавиш и пальцам:
          * ``'!'`` → ``(2, 'left_pinky')``,
          * ``'+'`` → ``(13, 'right_pinky')``,
          * ``'ъ'`` → ``(45, 'left_ring')``,
          * ``'ь'`` → ``(47, 'left_index')``.

        Используется для гарантии корректной генерации карты клавиш с учётом
        символов, доступных через Shift, при инициализации раскладки «Зубачёв».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('zubachev')
        shift = analyzer.shift_keys

        # Проверим несколько символов с Shift
        assert shift['!'] == (2, 'left_pinky')
        assert shift['+'] == (13, 'right_pinky')
        assert shift['ъ'] == (45, 'left_ring')
        assert shift['ь'] == (47, 'left_index')

    def test_home_positions(self):
        """
        Проверяет корректность словаря домашних позиций пальцев (``home_positions``)
        для раскладки ``zubachev`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку позиции левого указательного пальца (``left_index``) — ожидается код клавиши ``33``,
        - проверку позиции правого указательного пальца (``right_index``) — ожидается код клавиши ``23``,
        - проверку позиции правого большого пальца (``right_thumb``) — ожидается код клавиши ``57``.

        Используется для гарантии корректной инициализации домашнего ряда раскладки «Зубачёв».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('zubachev')
        home = analyzer.home_positions

        # Проверим домашние позиции
        assert home['left_index'] == 33
        assert home['right_index'] == 23
        assert home['right_thumb'] == 57

class TestSkoropisLayout:
    """
    Набор юнит‑тестов для проверки корректной инициализации раскладки ``skoropis``
    в классе ``KeyboardAnalyzer``.

    Включает:
    - тестирование основной карты клавиш (``keys``): проверка соответствия символов их кодам клавиш и пальцам,
    - проверку генерации заглавных букв (``caps_keys``): наличие корректных заглавных символов и отсутствие пробела,
    - проверку символов, вводимых через Shift (``shift_keys``): соответствие специальных символов ожидаемым позициям,
    - проверку позиций пальцев в домашнем ряду (``home_positions``): правильность кодов клавиш для указательных пальцев и большого пальца.

    Используется для гарантии корректной загрузки раскладки «Скоропись» и формирования
    всех вспомогательных структур, необходимых для анализа текста и эргономики набора.
    """
    def test_keys_mapping(self):
        """
        Проверяет корректность основной карты клавиш (``keys``) для раскладки ``skoropis``
        в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия конкретных символов их ожидаемым кодам клавиш и пальцам:
          * ``'ц'`` → ``(16, 'left_pinky')``,
          * ``'я'`` → ``(18, 'left_middle')``,
          * ``'к'`` → ``(23, 'right_index')``,
          * ``'ж'`` → ``(53, 'right_pinky')``,
          * пробел (``' '``) → ``(57, 'right_thumb')``.

        Используется для гарантии корректной загрузки раскладки «Скоропись» и формирования
        основной карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('skoropis')
        keys = analyzer.keys

        # Проверим несколько конкретных символов
        assert keys['ц'] == (16, 'left_pinky')
        assert keys['я'] == (18, 'left_middle')
        assert keys['к'] == (23, 'right_index')
        assert keys['ж'] == (53, 'right_pinky')
        assert keys[' '] == (57, 'right_thumb')

    def test_caps_keys_generation(self):
        """
        Проверяет корректность генерации словаря заглавных букв (``caps_keys``)
        для раскладки ``skoropis`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия заглавных символов их ожидаемым кодам клавиш и пальцам:
          * ``'Ц'`` → ``(16, 'left_pinky')``,
          * ``'Я'`` → ``(18, 'left_middle')``,
          * ``'Ж'`` → ``(53, 'right_pinky')``,
        - проверку отсутствия пробела (``' '``) в словаре ``caps_keys``,
          так как он не должен иметь заглавного варианта.

        Используется для гарантии корректной генерации заглавных букв при инициализации
        раскладки «Скоропись».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('skoropis')
        caps = analyzer.caps_keys

        # Проверим заглавные буквы
        assert caps['Ц'] == (16, 'left_pinky')
        assert caps['Я'] == (18, 'left_middle')
        assert caps['Ж'] == (53, 'right_pinky')
        assert ' ' not in caps

    def test_shift_keys_mapping(self):
        """
        Проверяет корректность словаря символов, вводимых через клавишу Shift (``shift_keys``)
        для раскладки ``skoropis`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия специальных символов их ожидаемым кодам клавиш и пальцам:
          * ``'.'`` → ``(2, 'left_pinky')``,
          * ``'ё'`` → ``(3, 'left_ring')``,
          * ``'!'`` → ``(6, 'right_index')``,
          * ``'«'`` → ``(13, 'right_pinky')``.

        Используется для гарантии корректной генерации карты клавиш с учётом
        символов, доступных через Shift, при инициализации раскладки «Скоропись».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('skoropis')
        shift = analyzer.shift_keys

        # Проверим несколько символов с Shift
        assert shift['.'] == (2, 'left_pinky')
        assert shift['ё'] == (3, 'left_ring')
        assert shift['!'] == (6, 'right_index')
        assert shift['«'] == (13, 'right_pinky')

    def test_home_positions(self):
        """
        Проверяет корректность словаря домашних позиций пальцев (``home_positions``)
        для раскладки ``skoropis`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку позиции левого указательного пальца (``left_index``) — ожидается код клавиши ``33``,
        - проверку позиции правого указательного пальца (``right_index``) — ожидается код клавиши ``23``,
        - проверку позиции правого большого пальца (``right_thumb``) — ожидается код клавиши ``57``.

        Используется для гарантии корректной инициализации домашнего ряда раскладки «Скоропись».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('skoropis')
        home = analyzer.home_positions

        # Проверим домашние позиции
        assert home['left_index'] == 33
        assert home['right_index'] == 23
        assert home['right_thumb'] == 57

class TestRusfonLayout:
    """
    Набор юнит‑тестов для проверки корректной инициализации раскладки ``rusfon``
    в классе ``KeyboardAnalyzer``.

    Включает:
    - тестирование основной карты клавиш (``keys``): проверка соответствия символов их кодам клавиш и пальцам,
    - проверку генерации заглавных букв (``caps_keys``): наличие корректных заглавных символов и отсутствие пробела,
    - проверку символов, вводимых через Shift (``shift_keys``): соответствие специальных символов ожидаемым позициям,
    - проверку позиций пальцев в домашнем ряду (``home_positions``): правильность кодов клавиш для указательных пальцев и большого пальца.

    Используется для гарантии корректной загрузки раскладки «Русфон» и формирования
    всех вспомогательных структур, необходимых для анализа текста и эргономики набора.
    """
    def test_keys_mapping(self):
        """
        Проверяет корректность основной карты клавиш (``keys``) для раскладки ``rusfon``
        в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия конкретных символов их ожидаемым кодам клавиш и пальцам:
          * ``'я'`` → ``(16, 'left_pinky')``,
          * ``'р'`` → ``(19, 'left_index')``,
          * ``'ш'`` → ``(26, 'right_index')``,
          * ``'/'`` → ``(53, 'right_pinky')``,
          * пробел (``' '``) → ``(57, 'right_thumb')``.

        Используется для гарантии корректной загрузки раскладки «Русфон» и формирования
        основной карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('rusfon')
        keys = analyzer.keys

        # Проверим несколько конкретных символов
        assert keys['я'] == (16, 'left_pinky')
        assert keys['р'] == (19, 'left_index')
        assert keys['ш'] == (26, 'right_index')
        assert keys['/'] == (53, 'right_pinky')
        assert keys[' '] == (57, 'right_thumb')

    def test_caps_keys_generation(self):
        """
        Проверяет корректность генерации словаря заглавных букв (``caps_keys``)
        для раскладки ``rusfon`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия заглавных символов их ожидаемым кодам клавиш и пальцам:
        * ``'Я'`` → ``(16, 'left_pinky')``,
        * ``'Р'`` → ``(19, 'left_index')``,
        * ``'Ш'`` → ``(26, 'right_index')``,
        - проверку отсутствия пробела (``' '``) в словаре ``caps_keys``,
          так как он не должен иметь заглавного варианта.

        Используется для гарантии корректной генерации заглавных букв при инициализации
        раскладки «Русфон».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('rusfon')
        caps = analyzer.caps_keys

        # Проверим заглавные буквы
        assert caps['Я'] == (16, 'left_pinky')
        assert caps['Р'] == (19, 'left_index')
        assert caps['Ш'] == (26, 'right_index')
        assert ' ' not in caps

    def test_shift_keys_mapping(self):
        """
        Проверяет корректность словаря символов, вводимых через клавишу Shift (``shift_keys``)
        для раскладки ``rusfon`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия специальных символов их ожидаемым кодам клавиш и пальцам:
          * ``'!'`` → ``(2, 'left_pinky')``,
          * ``'@'`` → ``(3, 'left_ring')``,
          * ``'Ё'`` → ``(5, 'left_index')``,
          * ``'?'`` → ``(53, 'right_pinky')``.

        Используется для гарантии корректной генерации карты клавиш с учётом
        символов, доступных через Shift, при инициализации раскладки «Русфон».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).

       """
        analyzer = KeyboardAnalyzer('rusfon')
        shift = analyzer.shift_keys

        # Проверим несколько символов с Shift
        assert shift['!'] == (2, 'left_pinky')
        assert shift['@'] == (3, 'left_ring')
        assert shift['Ё'] == (5, 'left_index')
        assert shift['?'] == (53, 'right_pinky')

    def test_home_positions(self):
        """
        Проверяет корректность словаря домашних позиций пальцев (``home_positions``)
        для раскладки ``rusfon`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку позиции левого указательного пальца (``left_index``) — ожидается код клавиши ``33``,
        - проверку позиции правого указательного пальца (``right_index``) — ожидается код клавиши ``23``,
        - проверку позиции правого большого пальца (``right_thumb``) — ожидается код клавиши ``57``.

        Используется для гарантии корректной инициализации домашнего ряда раскладки «Русфон».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('rusfon')
        home = analyzer.home_positions

        # Проверим домашние позиции
        assert home['left_index'] == 33
        assert home['right_index'] == 23
        assert home['right_thumb'] == 57
class TestDiktorLayout:
    """
    Набор юнит‑тестов для проверки корректной инициализации раскладки ``diktor``
    в классе ``KeyboardAnalyzer``.

    Включает:
    - тестирование основной карты клавиш (``keys``): проверка соответствия символов их кодам клавиш и пальцам,
    - проверку генерации заглавных букв (``caps_keys``): наличие корректных заглавных символов и отсутствие пробела,
    - проверку символов, вводимых через Shift (``shift_keys``): соответствие специальных символов ожидаемым позициям,
    - проверку позиций пальцев в домашнем ряду (``home_positions``): правильность кодов клавиш для указательных пальцев и большого пальца.

    Используется для гарантии корректной загрузки раскладки «Диктор» и формирования
    всех вспомогательных структур, необходимых для анализа текста и эргономики набора.
    """
    def test_keys_mapping(self):
        """
        Проверяет корректность основной карты клавиш (``keys``) для раскладки ``diktor``
        в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия конкретных символов их ожидаемым кодам клавиш и пальцам:
          * ``'ц'`` → ``(16, 'left_pinky')``,
          * ``'я'`` → ``(18, 'left_middle')``,
          * ``'к'`` → ``(23, 'right_index')``,
          * ``'ж'`` → ``(53, 'right_pinky')``,
          * ``'ё'`` → ``(41, 'right_pinky')``,
          * пробел (``' '``) → ``(57, 'right_thumb')``.

        Используется для гарантии корректной загрузки раскладки «Диктор» и формирования
        основной карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('diktor')
        keys = analyzer.keys

        # Проверим несколько конкретных символов
        assert keys['ц'] == (16, 'left_pinky')
        assert keys['я'] == (18, 'left_middle')
        assert keys['к'] == (23, 'right_index')
        assert keys['ж'] == (53, 'right_pinky')
        assert keys['ё'] == (41, 'right_pinky')
        assert keys[' '] == (57, 'right_thumb')

    def test_caps_keys_generation(self):
        """
        Проверяет корректность генерации словаря заглавных букв (``caps_keys``)
        для раскладки ``diktor`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия заглавных символов их ожидаемым кодам клавиш и пальцам:
          * ``'Ц'`` → ``(16, 'left_pinky')``,
          * ``'Я'`` → ``(18, 'left_middle')``,
          * ``'Ж'`` → ``(53, 'right_pinky')``,
          * ``'Ё'`` → ``(41, 'right_pinky')``,
        - проверку отсутствия пробела (``' '``) в словаре ``caps_keys``,
          так как он не должен иметь заглавного варианта.

        Используется для гарантии корректной генерации заглавных букв при инициализации
        раскладки «Диктор».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('diktor')
        caps = analyzer.caps_keys

        # Проверим заглавные буквы
        assert caps['Ц'] == (16, 'left_pinky')
        assert caps['Я'] == (18, 'left_middle')
        assert caps['Ж'] == (53, 'right_pinky')
        assert caps['Ё'] == (41, 'right_pinky')
        assert ' ' not in caps

    def test_shift_keys_mapping(self):
        """
        Проверяет корректность словаря символов, вводимых через клавишу Shift (``shift_keys``)
        для раскладки ``diktor`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия специальных символов их ожидаемым кодам клавиш и пальцам:
          * ``'ъ'`` → ``(17, 'left_ring')``,
          * ``'№'`` → ``(4, 'left_middle')``,
          * ``'!'`` → ``(20, 'left_index')``,
          * ``'+'`` → ``(13, 'right_pinky')``.

        Используется для гарантии корректной генерации карты клавиш с учётом
        символов, доступных через Shift, при инициализации раскладки «Диктор».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('diktor')
        shift = analyzer.shift_keys

        # Проверим несколько символов с Shift
        assert shift['ъ'] == (17, 'left_ring')
        assert shift['№'] == (4, 'left_middle')
        assert shift['!'] == (20, 'left_index')
        assert shift['+'] == (13, 'right_pinky')

    def test_home_positions(self):
        """
        Проверяет корректность словаря домашних позиций пальцев (``home_positions``)
        для раскладки ``diktor`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку позиции левого указательного пальца (``left_index``) — ожидается код клавиши ``33``,
        - проверку позиции правого указательного пальца (``right_index``) — ожидается код клавиши ``23``,
        - проверку позиции правого большого пальца (``right_thumb``) — ожидается код клавиши ``57``.

        Используется для гарантии корректной инициализации домашнего ряда раскладки «Диктор».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('diktor')
        home = analyzer.home_positions

        # Проверим домашние позиции
        assert home['left_index'] == 33
        assert home['right_index'] == 23
        assert home['right_thumb'] == 57

class TestAntLayout:
    """
    Набор юнит‑тестов для проверки корректной инициализации раскладки ``ant``
    в классе ``KeyboardAnalyzer``.

    Включает:
    - тестирование основной карты клавиш (``keys``): проверка соответствия символов их кодам клавиш и пальцам,
    - проверку генерации заглавных букв (``caps_keys``): наличие корректных заглавных символов и отсутствие пробела,
    - проверку символов, вводимых через Shift (``shift_keys``): соответствие специальных символов ожидаемым позициям,
    - проверку позиций пальцев в домашнем ряду (``home_positions``): правильность кодов клавиш для указательных пальцев и большого пальца.

    Используется для гарантии корректной загрузки раскладки «Ant» и формирования
    всех вспомогательных структур, необходимых для анализа текста и эргономики набора.
    """
    def test_keys_mapping(self):
        """
        Проверяет корректность основной карты клавиш (``keys``) для раскладки ``ant``
        в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия конкретных символов их ожидаемым кодам клавиш и пальцам:
          * ``'г'`` → ``(16, 'left_pinky')``,
          * ``'р'`` → ``(18, 'left_middle')``,
          * ``'я'`` → ``(23, 'right_index')``,
          * ``'ф'`` → ``(53, 'right_pinky')``,
          * пробел (``' '``) → ``(57, 'right_thumb')``.

        Используется для гарантии корректной загрузки раскладки «Ant» и формирования
        основной карты клавиш.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ant')
        keys = analyzer.keys

        # Проверим несколько конкретных символов
        assert keys['г'] == (16, 'left_pinky')
        assert keys['р'] == (18, 'left_middle')
        assert keys['я'] == (23, 'right_index')
        assert keys['ф'] == (53, 'right_pinky')
        assert keys[' '] == (57, 'right_thumb')

    def test_caps_keys_generation(self):
        """
        Проверяет корректность генерации словаря заглавных букв (``caps_keys``)
        для раскладки ``ant`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия заглавных символов их ожидаемым кодам клавиш и пальцам:
          * ``'Г'`` → ``(16, 'left_pinky')``,
          * ``'Р'`` → ``(18, 'left_middle')``,
          * ``'Я'`` → ``(23, 'right_index')``,
          * ``'Ф'`` → ``(53, 'right_pinky')``,
        - проверку отсутствия пробела (``' '``) в словаре ``caps_keys``,
          так как он не должен иметь заглавного варианта.

        Используется для гарантии корректной генерации заглавных букв при инициализации
        раскладки «Ant».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ant')
        caps = analyzer.caps_keys

        # Проверим заглавные буквы
        assert caps['Г'] == (16, 'left_pinky')
        assert caps['Р'] == (18, 'left_middle')
        assert caps['Я'] == (23, 'right_index')
        assert caps['Ф'] == (53, 'right_pinky')
        assert ' ' not in caps

    def test_shift_keys_mapping(self):
        """
        Проверяет корректность словаря символов, вводимых через клавишу Shift (``shift_keys``)
        для раскладки ``ant`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку соответствия специальных символов их ожидаемым кодам клавиш и пальцам:
          * ``'!'`` → ``(2, 'left_pinky')``,
          * ``'+'`` → ``(7, 'right_middle')``,
          * ``'/'`` → ``(10, 'right_pinky')``,
          * ``'»'`` → ``(13, 'right_pinky')``,
          * ``':'`` → ``(49, 'right_pinky')``.

        Используется для гарантии корректной генерации карты клавиш с учётом
        символов, доступных через Shift, при инициализации раскладки «Ant».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ant')
        shift = analyzer.shift_keys

        # Проверим несколько символов с Shift
        assert shift['!'] == (2, 'left_pinky')
        assert shift['+'] == (7, 'right_middle')
        assert shift['/'] == (10, 'right_pinky')
        assert shift['»'] == (13, 'right_pinky')
        assert shift[':'] == (49, 'right_pinky')

    def test_home_positions(self):
        """
        Проверяет корректность словаря домашних позиций пальцев (``home_positions``)
        для раскладки ``ant`` в классе ``KeyboardAnalyzer``.

        Включает:
        - проверку позиции левого указательного пальца (``left_index``) — ожидается код клавиши ``33``,
        - проверку позиции правого указательного пальца (``right_index``) — ожидается код клавиши ``23``,
        - проверку позиции правого большого пальца (``right_thumb``) — ожидается код клавиши ``57``.

        Используется для гарантии корректной инициализации домашнего ряда раскладки «Ant».

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ant')
        home = analyzer.home_positions

        # Проверим домашние позиции
        assert home['left_index'] == 33
        assert home['right_index'] == 23
        assert home['right_thumb'] == 57
class TestPenaltyCalculation:
    """
    Набор юнит‑тестов для проверки корректности вычисления штрафного пути
    методом ``_calculate_path`` в классе ``KeyboardAnalyzer``.

    Каждый тест:
    - создаёт объект ``KeyboardAnalyzer`` с конкретной раскладкой,
    - выбирает символ и его назначенный палец,
    - вычисляет ожидаемое значение пути (манхэттенское расстояние между
      фактической позицией клавиши и домашней позицией пальца),
    - сравнивает результат метода с ожидаемым значением.

    Используется для гарантии корректной работы алгоритма расчёта
    эргономических штрафов при наборе текста.
    """
    def test_ytsuken(self):
        """
        Проверяет корректность расчёта штрафного пути методом ``_calculate_path``
        для раскладки ``ytsuken`` в классе ``KeyboardAnalyzer``.

        Символ: ``'й'`` → код клавиши ``16``, палец ``left_pinky``.

        Ожидаемое значение:
            - фактическая позиция клавиши: (ряд=1, колонка=0),
            - домашняя позиция пальца: (ряд=2, колонка=0),
            - манхэттенское расстояние: ``abs(1 - 2) + abs(0 - 0) = 1``.

        Используется для проверки, что алгоритм корректно вычисляет расстояние
        между фактической позицией клавиши и домашней позицией пальца.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ytsuken')
        code, finger = analyzer.keys['й']  # (16, 'left_pinky')
        expected = abs(1 - 2) + abs(0 - 0)  # (ряд, колонка): (1,0) vs (2,0)
        actual = analyzer._calculate_path(code, finger)
        assert actual == expected

    def test_vyzov(self):
        """
        Проверяет корректность расчёта штрафного пути методом ``_calculate_path``
        для раскладки ``vyzov`` в классе ``KeyboardAnalyzer``.

        Символ: ``'а'`` → код клавиши ``33``, палец ``left_index``.

        Ожидаемое значение:
            - фактическая позиция клавиши: (ряд=2, колонка=3),
            - домашняя позиция пальца: (ряд=2, колонка=3),
            - манхэттенское расстояние: ``abs(2 - 2) + abs(3 - 3) = 0``.

        Используется для проверки, что алгоритм корректно вычисляет расстояние
        между фактической позицией клавиши и домашней позицией пальца.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('vyzov')
        code, finger = analyzer.keys['а']  # например (33, 'left_index')
        expected = abs(2 - 2) + abs(3 - 3)  # (2,3) vs (2,3)
        actual = analyzer._calculate_path(code, finger)
        assert actual == expected

    def test_zubachev(self):
        """
        Проверяет корректность расчёта штрафного пути методом ``_calculate_path``
        для раскладки ``zubachev`` в классе ``KeyboardAnalyzer``.

        Символ: ``'о'`` → код клавиши ``36``, палец ``right_index``.

        Ожидаемое значение:
            - фактическая позиция клавиши: (ряд=2, колонка=6),
            - домашняя позиция пальца: (ряд=2, колонка=6),
            - манхэттенское расстояние: ``abs(2 - 2) + abs(6 - 6) = 0``.

        Используется для проверки, что алгоритм корректно вычисляет расстояние
        между фактической позицией клавиши и домашней позицией пальца.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('zubachev')
        code, finger = analyzer.keys['о']  # например (36, 'right_index')
        expected = abs(2 - 2) + abs(6 - 6)  # (2,6) vs (2,6)
        actual = analyzer._calculate_path(code, finger)
        assert actual == expected

    def test_skoropis(self):
        """
        Проверяет корректность расчёта штрафного пути методом ``_calculate_path``
        для раскладки ``skoropis`` в классе ``KeyboardAnalyzer``.

        Символ: ``'ц'`` → код клавиши ``17``, палец ``left_ring``.

        Ожидаемое значение:
            - фактическая позиция клавиши: (ряд=1, колонка=1),
            - домашняя позиция пальца: (ряд=2, колонка=1),
            - манхэттенское расстояние: ``abs(1 - 2) + abs(1 - 1) = 1``.

        Используется для проверки, что алгоритм корректно вычисляет расстояние
        между фактической позицией клавиши и домашней позицией пальца.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('skoropis')
        code, finger = analyzer.keys['ц']  # например (17, 'left_ring')
        expected = abs(1 - 2) + abs(1 - 1)  # (1,1) vs (2,1)
        actual = analyzer._calculate_path(code, finger)
        assert actual == expected

    def test_rusfon(self):
        """
        Проверяет корректность расчёта штрафного пути методом ``_calculate_path``
        для раскладки ``rusfon`` в классе ``KeyboardAnalyzer``.

        Символ: ``'р'`` → код клавиши ``35``, палец ``right_index``.

        Ожидаемое значение:
            - фактическая позиция клавиши: (ряд=2, колонка=5),
            - домашняя позиция пальца: (ряд=2, колонка=6),
            - манхэттенское расстояние: ``abs(2 - 2) + abs(5 - 6) = 1``.

        Используется для проверки, что алгоритм корректно вычисляет расстояние
        между фактической позицией клавиши и домашней позицией пальца.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('rusfon')
        code, finger = analyzer.keys['р']  # например (35, 'right_index')
        expected = abs(2 - 2) + abs(5 - 6)  # (2,5) vs (2,6)
        actual = analyzer._calculate_path(code, finger)
        assert actual == expected

    def test_diktor(self):
        """
        Проверяет корректность расчёта штрафного пути методом ``_calculate_path``
        для раскладки ``diktor`` в классе ``KeyboardAnalyzer``.

        Символ: ``'д'`` → код клавиши ``24``, палец ``right_index``.

        Ожидаемое значение:
            - фактическая позиция клавиши: (ряд=1, колонка=8),
            - домашняя позиция пальца: (ряд=1, колонка=7),
            - манхэттенское расстояние: ``abs(1 - 1) + abs(8 - 7) = 1``.

        Используется для проверки, что алгоритм корректно вычисляет расстояние
        между фактической позицией клавиши и домашней позицией пальца.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('diktor')
        code, finger = analyzer.keys['д']  # например (24, 'right_index')
        expected = abs(1 - 1) + abs(8 - 7)  # (1,8) vs (1,7)
        actual = analyzer._calculate_path(code, finger)
        assert actual == expected

    def test_ant(self):
        """
        Проверяет корректность расчёта штрафного пути методом ``_calculate_path``
        для раскладки ``ant`` в классе ``KeyboardAnalyzer``.

        Символ: ``'г'`` → код клавиши ``16``, палец ``left_pinky``.

        Ожидаемое значение:
            - фактическая позиция клавиши: (ряд=1, колонка=0),
            - домашняя позиция пальца: (ряд=2, колонка=0),
            - манхэттенское расстояние: ``abs(1 - 2) + abs(0 - 0) = 1``.

        Используется для проверки, что алгоритм корректно вычисляет расстояние
        между фактической позицией клавиши и домашней позицией пальца.

        Входные данные (точка входа):
            None: Тест не принимает аргументов, создаёт объект ``KeyboardAnalyzer``.

        Выходные данные (точка выхода):
            None: Проверка осуществляется через утверждения (``assert``).
        """
        analyzer = KeyboardAnalyzer('ant')
        code, finger = analyzer.keys['г']  # например (16, 'left_pinky')
        expected = abs(1 - 2) + abs(0 - 0)  # (1,0) vs (2,0)
        actual = analyzer._calculate_path(code, finger)
        assert actual == expected

class TestAnalyzeText:
    """
    Набор юнит‑тестов для проверки корректности анализа текста методом
    ``analyze_text`` в классе ``KeyboardAnalyzer``.

    Каждый тест:
    - создаёт объект ``KeyboardAnalyzer`` с конкретной раскладкой,
    - анализирует строку с заглавными буквами, символами Shift или Alt,
    - проверяет, что результат содержит корректные метрики:
      * имя текста (``text_name``),
      * количество проанализированных символов (``characters_analyzed``),
      * количество нажатий с модификаторами (Shift/Alt),
      * общее количество нажатий (``total_presses``),
      * распределение по рукам (``two_handed``, ``left_hand_count``, ``right_hand_count``).

    Используется для гарантии корректной работы анализа текста с учётом
    модификаторов в разных раскладках.
    """
    def test_analysis_with_modifiers_ytsuken(self):
        """
        Проверяет корректность анализа текста с модификаторами для раскладки ``ytsuken``
        методом ``analyze_text`` в классе ``KeyboardAnalyzer``.

        Текст: ``"ПрИвЕт, Мир!"`` — содержит заглавные буквы и символы, вводимые с Shift.

        Проверяется:
        - корректность имени текста (ключ ``text_name``),
        - количество проанализированных символов (ключ ``characters_analyzed``),
        - наличие нажатий с Shift (ключ ``shift_count`` > 0),
        - корректность общего числа нажатий (ключ ``total_presses`` ≥ количество символов),
        - наличие двуручного ввода (ключ ``two_handed`` > 0),
        - распределение нагрузки между руками:
            * ``left_hand_count`` — количество нажатий левой рукой,
            * ``right_hand_count`` — количество нажатий правой рукой.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``ytsuken``
            - строка текста: ``"ПрИвЕт, Мир!"``
            - имя текста: ``"Модификаторы ЙЦУКЕН"``

        Выходные данные (точка выхода):
            - словарь результата анализа (``dict``) со следующими ключами:
                * ``text_name`` — имя текста для отчёта,
                * ``characters_analyzed`` — количество символов в анализируемой строке,
                * ``shift_count`` — число нажатий с использованием Shift,
                * ``alt_count`` — число нажатий с использованием Alt (если применимо),
                * ``total_presses`` — общее количество нажатий клавиш,
                * ``two_handed`` — число случаев двуручного ввода,
                * ``left_hand_count`` — количество нажатий левой рукой,
                * ``right_hand_count`` — количество нажатий правой рукой.
        """
        analyzer = KeyboardAnalyzer('ytsuken')
        text = "ПрИвЕт, Мир!"  # Заглавные буквы и символы с Shift
        result = analyzer.analyze_text(text, "Модификаторы ЙЦУКЕН")

        assert result is not None
        assert result['text_name'] == "Модификаторы ЙЦУКЕН"
        assert result['characters_analyzed'] == len("ПрИвЕт, Мир!")
        assert result['shift_count'] > 0
        assert result['total_presses'] >= result['characters_analyzed']
        assert result['two_handed'] > 0
        assert result['left_hand_count'] > 0
        assert result['right_hand_count'] > 0

    def test_analysis_with_modifiers_vyzov(self):
        """
        Проверяет корректность анализа текста с модификаторами для раскладки ``vyzov``
        методом ``analyze_text`` в классе ``KeyboardAnalyzer``.

        Текст: ``"цщъэ! Привет"`` — содержит символы, вводимые через Alt, и символы с Shift.

        Проверяется:
        - корректность имени текста (ключ ``text_name``),
        - количество проанализированных символов (ключ ``characters_analyzed``),
        - наличие нажатий с Shift (ключ ``shift_count`` > 0),
        - наличие нажатий с Alt (ключ ``alt_count`` > 0),
        - корректность общего числа нажатий (ключ ``total_presses`` ≥ количество символов),
        - наличие двуручного ввода (ключ ``two_handed`` > 0),
        - распределение нагрузки между руками:
            * ``left_hand_count`` — количество нажатий левой рукой,
            * ``right_hand_count`` — количество нажатий правой рукой.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``vyzov``
            - строка текста: ``"цщъэ! Привет"``
            - имя текста: ``"Модификаторы Вызов"``

        Выходные данные (точка выхода):
            - словарь результата анализа (``dict``) со следующими ключами:
                * ``text_name`` — имя текста для отчёта,
                * ``characters_analyzed`` — количество символов в анализируемой строке,
                * ``shift_count`` — число нажатий с использованием Shift,
                * ``alt_count`` — число нажатий с использованием Alt,
                * ``total_presses`` — общее количество нажатий клавиш,
                * ``two_handed`` — число случаев двуручного ввода,
                * ``left_hand_count`` — количество нажатий левой рукой,
                * ``right_hand_count`` — количество нажатий правой рукой.
        """
        analyzer = KeyboardAnalyzer('vyzov')
        text = "цщъэ! Привет"  # Alt-символы + Shift
        result = analyzer.analyze_text(text, "Модификаторы Вызов")

        assert result is not None
        assert result['text_name'] == "Модификаторы Вызов"
        assert result['characters_analyzed'] == len("цщъэ! Привет")
        assert result['shift_count'] > 0
        assert result['alt_count'] > 0
        assert result['total_presses'] >= result['characters_analyzed']
        assert result['two_handed'] > 0
        assert result['left_hand_count'] > 0
        assert result['right_hand_count'] > 0

    def test_analysis_with_modifiers_zubachev(self):
        """
        Проверяет корректность анализа текста с модификаторами для раскладки ``zubachev``
        методом ``analyze_text`` в классе ``KeyboardAnalyzer``.

        Текст: ``"ФЫА! Привет?"`` — содержит заглавные буквы и символы, вводимые с Shift.

        Проверяется:
        - корректность имени текста (ключ ``text_name``),
        - количество проанализированных символов (ключ ``characters_analyzed``),
        - наличие нажатий с Shift (ключ ``shift_count`` > 0),
        - корректность общего числа нажатий (ключ ``total_presses`` ≥ количество символов),
        - наличие двуручного ввода (ключ ``two_handed`` > 0),
        - распределение нагрузки между руками:
            * ``left_hand_count`` — количество нажатий левой рукой,
            * ``right_hand_count`` — количество нажатий правой рукой.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``zubachev``
            - строка текста: ``"ФЫА! Привет?"``
            - имя текста: ``"Модификаторы Зубачев"``

        Выходные данные (точка выхода):
            - словарь результата анализа (``dict``) со следующими ключами:
                * ``text_name`` — имя текста для отчёта,
                * ``characters_analyzed`` — количество символов в анализируемой строке,
                * ``shift_count`` — число нажатий с использованием Shift,
                * ``alt_count`` — число нажатий с использованием Alt (если применимо),
                * ``total_presses`` — общее количество нажатий клавиш,
                * ``two_handed`` — число случаев двуручного ввода,
                * ``left_hand_count`` — количество нажатий левой рукой,
                * ``right_hand_count`` — количество нажатий правой рукой.
        """
        analyzer = KeyboardAnalyzer('zubachev')
        text = "ФЫА! Привет?"  # Заглавные + Shift
        result = analyzer.analyze_text(text, "Модификаторы Зубачев")

        assert result is not None
        assert result['text_name'] == "Модификаторы Зубачев"
        assert result['characters_analyzed'] == len("ФЫА! Привет?")
        assert result['shift_count'] > 0
        assert result['total_presses'] >= result['characters_analyzed']
        assert result['two_handed'] > 0
        assert result['left_hand_count'] > 0
        assert result['right_hand_count'] > 0

    def test_analysis_with_modifiers_skoropis(self):
        """
        Проверяет корректность анализа текста с модификаторами для раскладки ``skoropis``
        методом ``analyze_text`` в классе ``KeyboardAnalyzer``.

        Текст: ``"ЦА! Привет?"`` — содержит заглавные буквы и символы, вводимые с Shift.

        Проверяется:
        - корректность имени текста (ключ ``text_name``),
        - количество проанализированных символов (ключ ``characters_analyzed``),
        - наличие нажатий с Shift (ключ ``shift_count`` > 0),
        - корректность общего числа нажатий (ключ ``total_presses`` ≥ количество символов),
        - наличие двуручного ввода (ключ ``two_handed`` > 0),
        - распределение нагрузки между руками:
            * ``left_hand_count`` — количество нажатий левой рукой,
            * ``right_hand_count`` — количество нажатий правой рукой.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``skoropis``
            - строка текста: ``"ЦА! Привет?"``
            - имя текста: ``"Модификаторы Скоропись"``

        Выходные данные (точка выхода):
            - словарь результата анализа (``dict``) со следующими ключами:
                * ``text_name`` — имя текста для отчёта,
                * ``characters_analyzed`` — количество символов в анализируемой строке,
                * ``shift_count`` — число нажатий с использованием Shift,
                * ``alt_count`` — число нажатий с использованием Alt (если применимо),
                * ``total_presses`` — общее количество нажатий клавиш,
                * ``two_handed`` — число случаев двуручного ввода,
                * ``left_hand_count`` — количество нажатий левой рукой,
                * ``right_hand_count`` — количество нажатий правой рукой.
        """
        analyzer = KeyboardAnalyzer('skoropis')
        text = "ЦА! Привет?"  # Заглавные + Shift
        result = analyzer.analyze_text(text, "Модификаторы Скоропись")

        assert result is not None
        assert result['text_name'] == "Модификаторы Скоропись"
        assert result['characters_analyzed'] == len("ЦА! Привет?")
        assert result['shift_count'] > 0
        assert result['total_presses'] >= result['characters_analyzed']
        assert result['two_handed'] > 0
        assert result['left_hand_count'] > 0
        assert result['right_hand_count'] > 0

    def test_analysis_with_modifiers_rusfon(self):
        """
        Проверяет корректность анализа текста с модификаторами для раскладки ``rusfon``
        методом ``analyze_text`` в классе ``KeyboardAnalyzer``.

        Текст: ``"ЯВЕ! Привет?"`` — содержит заглавные буквы и символы, вводимые с Shift.

        Проверяется:
        - корректность имени текста (ключ ``text_name``),
        - количество проанализированных символов (ключ ``characters_analyzed``),
        - наличие нажатий с Shift (ключ ``shift_count`` > 0),
        - корректность общего числа нажатий (ключ ``total_presses`` ≥ количество символов),
        - наличие двуручного ввода (ключ ``two_handed`` > 0),
        - распределение нагрузки между руками:
            * ``left_hand_count`` — количество нажатий левой рукой,
            * ``right_hand_count`` — количество нажатий правой рукой.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``rusfon``
            - строка текста: ``"ЯВЕ! Привет?"``
            - имя текста: ``"Модификаторы Русфон"``

        Выходные данные (точка выхода):
            - словарь результата анализа (``dict``) со следующими ключами:
                * ``text_name`` — имя текста для отчёта,
                * ``characters_analyzed`` — количество символов в анализируемой строке,
                * ``shift_count`` — число нажатий с использованием Shift,
                * ``alt_count`` — число нажатий с использованием Alt (если применимо),
                * ``total_presses`` — общее количество нажатий клавиш,
                * ``two_handed`` — число случаев двуручного ввода,
                * ``left_hand_count`` — количество нажатий левой рукой,
                * ``right_hand_count`` — количество нажатий правой рукой.
        """
        analyzer = KeyboardAnalyzer('rusfon')
        text = "ЯВЕ! Привет?"  # Заглавные + Shift
        result = analyzer.analyze_text(text, "Модификаторы Русфон")

        assert result is not None
        assert result['text_name'] == "Модификаторы Русфон"
        assert result['characters_analyzed'] == len("ЯВЕ! Привет?")
        assert result['shift_count'] > 0
        assert result['total_presses'] >= result['characters_analyzed']
        assert result['two_handed'] > 0
        assert result['left_hand_count'] > 0
        assert result['right_hand_count'] > 0

    def test_analysis_with_modifiers_diktor(self):
        """
        Проверяет корректность анализа текста с модификаторами для раскладки ``diktor``
        методом ``analyze_text`` в классе ``KeyboardAnalyzer``.

        Текст: ``"ЦА! Привет?"`` — содержит заглавные буквы и символы, вводимые с Shift.

        Проверяется:
        - корректность имени текста (ключ ``text_name``),
        - количество проанализированных символов (ключ ``characters_analyzed``),
        - наличие нажатий с Shift (ключ ``shift_count`` > 0),
        - корректность общего числа нажатий (ключ ``total_presses`` ≥ количество символов),
        - наличие двуручного ввода (ключ ``two_handed`` > 0),
        - распределение нагрузки между руками:
            * ``left_hand_count`` — количество нажатий левой рукой,
            * ``right_hand_count`` — количество нажатий правой рукой.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``diktor``
            - строка текста: ``"ЦА! Привет?"``
            - имя текста: ``"Модификаторы Диктор"``

        Выходные данные (точка выхода):
            - словарь результата анализа (``dict``) со следующими ключами:
                * ``text_name`` — имя текста для отчёта,
                * ``characters_analyzed`` — количество символов в анализируемой строке,
                * ``shift_count`` — число нажатий с использованием Shift,
                * ``alt_count`` — число нажатий с использованием Alt (если применимо),
                * ``total_presses`` — общее количество нажатий клавиш,
                * ``two_handed`` — число случаев двуручного ввода,
                * ``left_hand_count`` — количество нажатий левой рукой,
                * ``right_hand_count`` — количество нажатий правой рукой.
        """
        analyzer = KeyboardAnalyzer('diktor')
        text = "ЦА! Привет?"  # Заглавные + Shift
        result = analyzer.analyze_text(text, "Модификаторы Диктор")

        assert result is not None
        assert result['text_name'] == "Модификаторы Диктор"
        assert result['characters_analyzed'] == len("ЦА! Привет?")
        assert result['shift_count'] > 0
        assert result['total_presses'] >= result['characters_analyzed']
        assert result['two_handed'] > 0
        assert result['left_hand_count'] > 0
        assert result['right_hand_count'] > 0

    def test_analysis_with_modifiers_ant(self):
        """
        Проверяет корректность анализа текста с модификаторами для раскладки ``ant``
        методом ``analyze_text`` в классе ``KeyboardAnalyzer``.

        Текст: ``"ГПР! Привет?"`` — содержит заглавные буквы и символы, вводимые с Shift.

        Проверяется:
        - корректность имени текста (ключ ``text_name``),
        - количество проанализированных символов (ключ ``characters_analyzed``),
        - наличие нажатий с Shift (ключ ``shift_count`` > 0),
        - корректность общего числа нажатий (ключ ``total_presses`` ≥ количество символов),
        - наличие двуручного ввода (ключ ``two_handed`` > 0),
        - распределение нагрузки между руками:
            * ``left_hand_count`` — количество нажатий левой рукой,
            * ``right_hand_count`` — количество нажатий правой рукой.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``ant``
            - строка текста: ``"ГПР! Привет?"``
            - имя текста: ``"Модификаторы Ант"``

        Выходные данные (точка выхода):
            - словарь результата анализа (``dict``) со следующими ключами:
                * ``text_name`` — имя текста для отчёта,
                * ``characters_analyzed`` — количество символов в анализируемой строке,
                * ``shift_count`` — число нажатий с использованием Shift,
                * ``alt_count`` — число нажатий с использованием Alt (если применимо),
                * ``total_presses`` — общее количество нажатий клавиш,
                * ``two_handed`` — число случаев двуручного ввода,
                * ``left_hand_count`` — количество нажатий левой рукой,
                * ``right_hand_count`` — количество нажатий правой рукой.
        """
        analyzer = KeyboardAnalyzer('ant')
        text = "ГПР! Привет?"  # Заглавные + Shift
        result = analyzer.analyze_text(text, "Модификаторы Ант")

        assert result is not None
        assert result['text_name'] == "Модификаторы Ант"
        assert result['characters_analyzed'] == len("ГПР! Привет?")
        assert result['shift_count'] > 0
        assert result['total_presses'] >= result['characters_analyzed']
        assert result['two_handed'] > 0
        assert result['left_hand_count'] > 0
        assert result['right_hand_count'] > 0

class TestLoadTextFileAllLayouts:
    """
    Набор юнит‑тестов для проверки корректности работы метода
    ``_load_text_file`` и сигнатуры метода ``analyze_text`` в классе
    ``KeyboardAnalyzer``.

    Включает:
    - тесты загрузки существующих временных файлов для всех раскладок,
    - тесты обработки отсутствующих или некорректных аргументов,
    - тесты проверки аргументов и аннотаций метода ``analyze_text``.

    Используется для гарантии корректной работы ввода текстов из файлов
    и правильности интерфейса анализа текста.
    """
    def test_load_existing_file_ytsuken(self):
        """
        Проверяет корректность загрузки существующего текстового файла методом
        ``_load_text_file`` для раскладки ``ytsuken`` в классе ``KeyboardAnalyzer``.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``ytsuken``
            - временный файл с содержимым: ``"Привет, Ytsuken!"``

        Выходные данные (точка выхода):
            - строка: ``"Привет, Ytsuken!"``

        Проверяется:
            - метод корректно читает содержимое файла,
            - возвращаемая строка совпадает с записанным текстом.
        """
        analyzer = KeyboardAnalyzer('ytsuken')
        with tempfile.NamedTemporaryFile('w+', encoding='utf-8', delete=False) as tmp:
            tmp.write("Привет, Ytsuken!")
            tmp_filename = tmp.name
        try:
            result = analyzer._load_text_file(tmp_filename)
            assert result == "Привет, Ytsuken!"
        finally:
            os.remove(tmp_filename)

    def test_load_existing_file_vyzov(self):
        """
        Проверяет корректность загрузки существующего текстового файла методом
        ``_load_text_file`` для раскладки ``vyzov`` в классе ``KeyboardAnalyzer``.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``vyzov``
            - временный файл с содержимым: ``"Привет, Vyzov!"``

        Выходные данные (точка выхода):
            - строка: ``"Привет, Vyzov!"``

        Проверяется:
            - метод корректно читает содержимое файла,
            - возвращаемая строка совпадает с записанным текстом.
        """
        analyzer = KeyboardAnalyzer('vyzov')
        with tempfile.NamedTemporaryFile('w+', encoding='utf-8', delete=False) as tmp:
            tmp.write("Привет, Vyzov!")
            tmp_filename = tmp.name
        try:
            result = analyzer._load_text_file(tmp_filename)
            assert result == "Привет, Vyzov!"
        finally:
            os.remove(tmp_filename)

    def test_load_existing_file_zubachev(self):
        """
        Проверяет корректность загрузки существующего текстового файла методом
        ``_load_text_file`` для раскладки ``zubachev`` в классе ``KeyboardAnalyzer``.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``zubachev``
            - временный файл с содержимым: ``"Привет, Zubachev!"``

        Выходные данные (точка выхода):
            - строка: ``"Привет, Zubachev!"``

        Проверяется:
            - метод корректно читает содержимое файла,
            - возвращаемая строка совпадает с записанным текстом.
        """
        analyzer = KeyboardAnalyzer('zubachev')
        with tempfile.NamedTemporaryFile('w+', encoding='utf-8', delete=False) as tmp:
            tmp.write("Привет, Zubachev!")
            tmp_filename = tmp.name
        try:
            result = analyzer._load_text_file(tmp_filename)
            assert result == "Привет, Zubachev!"
        finally:
            os.remove(tmp_filename)

    def test_load_existing_file_skoropis(self):
        """
        Проверяет корректность загрузки существующего текстового файла методом
        ``_load_text_file`` для раскладки ``skoropis`` в классе ``KeyboardAnalyzer``.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``skoropis``
            - временный файл с содержимым: ``"Привет, Скоропись!"``

        Выходные данные (точка выхода):
            - строка: ``"Привет, Скоропись!"``

        Проверяется:
            - метод корректно читает содержимое файла,
            - возвращаемая строка совпадает с записанным текстом.
        """
        analyzer = KeyboardAnalyzer('skoropis')
        with tempfile.NamedTemporaryFile('w+', encoding='utf-8', delete=False) as tmp:
            tmp.write("Привет, Скоропись!")
            tmp_filename = tmp.name
        try:
            result = analyzer._load_text_file(tmp_filename)
            assert result == "Привет, Скоропись!"
        finally:
            os.remove(tmp_filename)

    def test_load_existing_file_rusfon(self):
        """
        Проверяет корректность загрузки существующего текстового файла методом
        ``_load_text_file`` для раскладки ``rusfon`` в классе ``KeyboardAnalyzer``.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``rusfon``
            - временный файл с содержимым: ``"Привет, Русфон!"``

        Выходные данные (точка выхода):
            - строка: ``"Привет, Русфон!"``

        Проверяется:
            - метод корректно читает содержимое файла,
            - возвращаемая строка совпадает с записанным текстом.
        """
        analyzer = KeyboardAnalyzer('rusfon')
        with tempfile.NamedTemporaryFile('w+', encoding='utf-8', delete=False) as tmp:
            tmp.write("Привет, Русфон!")
            tmp_filename = tmp.name
        try:
            result = analyzer._load_text_file(tmp_filename)
            assert result == "Привет, Русфон!"
        finally:
            os.remove(tmp_filename)

    def test_load_existing_file_diktor(self):
        """
        Проверяет корректность загрузки существующего текстового файла методом
        ``_load_text_file`` для раскладки ``diktor`` в классе ``KeyboardAnalyzer``.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``diktor``
            - временный файл с содержимым: ``"Привет, Диктор!"``

        Выходные данные (точка выхода):
            - строка: ``"Привет, Диктор!"``

        Проверяется:
            - метод корректно читает содержимое файла,
            - возвращаемая строка совпадает с записанным текстом.
        """
        analyzer = KeyboardAnalyzer('diktor')
        with tempfile.NamedTemporaryFile('w+', encoding='utf-8', delete=False) as tmp:
            tmp.write("Привет, Диктор!")
            tmp_filename = tmp.name
        try:
            result = analyzer._load_text_file(tmp_filename)
            assert result == "Привет, Диктор!"
        finally:
            os.remove(tmp_filename)

    def test_load_existing_file_ant(self):
        """
        Проверяет корректность загрузки существующего текстового файла методом
        ``_load_text_file`` для раскладки ``ant`` в классе ``KeyboardAnalyzer``.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``ant``
            - временный файл с содержимым: ``"Привет, Ант!"``

        Выходные данные (точка выхода):
            - строка: ``"Привет, Ант!"``

        Проверяется:
            - метод корректно читает содержимое файла,
            - возвращаемая строка совпадает с записанным текстом.
        """
        analyzer = KeyboardAnalyzer('ant')
        with tempfile.NamedTemporaryFile('w+', encoding='utf-8', delete=False) as tmp:
            tmp.write("Привет, Ант!")
            tmp_filename = tmp.name
        try:
            result = analyzer._load_text_file(tmp_filename)
            assert result == "Привет, Ант!"
        finally:
            os.remove(tmp_filename)

    def test_load_missing_file(self):
        """
        Проверяет корректность обработки отсутствующего файла методом
        ``_load_text_file`` в классе ``KeyboardAnalyzer``.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``ytsuken``
            - путь к несуществующему файлу: ``"несуществующий_файл.txt"``

        Выходные данные (точка выхода):
            - пустая строка: ``""``

        Проверяется:
            - метод не вызывает исключений при попытке загрузки отсутствующего файла,
            - возвращаемое значение соответствует ожидаемому (пустая строка).
        """
        analyzer = KeyboardAnalyzer('ytsuken')
        result = analyzer._load_text_file("несуществующий_файл.txt")
        assert result == ""

    def test_load_invalid_argument(self):
        """
        Проверяет корректность обработки некорректного аргумента методом
        ``_load_text_file`` в классе ``KeyboardAnalyzer``.

        Входные данные (точка входа):
            - объект ``KeyboardAnalyzer`` с раскладкой ``ytsuken``
            - аргумент: ``None`` вместо пути к файлу

        Выходные данные (точка выхода):
            - ``None`` или пустая строка: ``""``

        Проверяется:
            - метод не вызывает исключений при передаче некорректного аргумента,
            - возвращаемое значение соответствует ожидаемому (``None`` или пустая строка).
        """
        analyzer = KeyboardAnalyzer('ytsuken')
        result = analyzer._load_text_file(None)
        assert result is None or result == ""

    def test_analyze_text_argument_names(self):
        """
        Проверяет корректность имён аргументов метода ``analyze_text`` в классе
        ``KeyboardAnalyzer`` с использованием introspection через модуль ``inspect``.

        Входные данные (точка входа):
            - объект сигнатуры метода ``KeyboardAnalyzer.analyze_text``
            - параметры, извлечённые через ``inspect.signature``

        Выходные данные (точка выхода):
            - список имён аргументов: ``['self', 'text', 'text_name', 'common_chars']``

        Проверяется:
            - порядок и названия аргументов метода соответствуют ожидаемым:
                * ``self`` — ссылка на экземпляр класса,
                * ``text`` — анализируемая строка текста,
                * ``text_name`` — имя текста для отчёта,
                * ``common_chars`` — множество общих символов для анализа.
        """
        sig = inspect.signature(KeyboardAnalyzer.analyze_text)
        params = sig.parameters

        assert list(params.keys()) == ['self', 'text', 'text_name', 'common_chars']

    def test_analyze_text_annotations_if_present(self):
        """
        Проверяет наличие и корректность аннотаций аргументов метода ``analyze_text``
        в классе ``KeyboardAnalyzer`` с использованием introspection через модуль ``inspect``.

        Входные данные (точка входа):
            - объект сигнатуры метода ``KeyboardAnalyzer.analyze_text``
            - параметры, извлечённые через ``inspect.signature``

        Выходные данные (точка выхода):
            - если аннотации указаны:
                * ``text`` — должен быть типа ``str``
                * ``text_name`` — должен быть типа ``str``
                * ``common_chars`` — должен быть множеством: ``set[str]``, ``set`` или ``set | None``
            - если аннотации отсутствуют:
                * выводится диагностическое сообщение в консоль

        Проверяется:
            - наличие аннотаций для каждого аргумента,
            - соответствие типов аннотаций ожидаемым значениям,
            - корректная обработка случая, когда аннотация не указана.
        """
        sig = inspect.signature(KeyboardAnalyzer.analyze_text)
        params = sig.parameters

        if params['text'].annotation is not inspect._empty:
            assert params['text'].annotation == str, "text должен быть типа str"
        else:
            print("Аннотация для text не указана")

        if params['text_name'].annotation is not inspect._empty:
            assert params['text_name'].annotation == str, "text_name должен быть типа str"
        else:
            print("Аннотация для text_name не указана")

        if params['common_chars'].annotation is not inspect._empty:
            assert params['common_chars'].annotation in [set[str], set, set | None], "common_chars должен быть множеством"
        else:
            print("Аннотация для common_chars не указана")

class TestAnalyzeAllFiles:
    def create_temp_files(self):
        temp_dir = tempfile.TemporaryDirectory()
        file1 = os.path.join(temp_dir.name, 'voina_i_mir.txt')
        file2 = os.path.join(temp_dir.name, 'sortchbukw.csv')
        file3 = os.path.join(temp_dir.name, '1grams.txt')

        with open(file1, 'w', encoding='utf-8') as f:
            f.write("Все смешалось в доме Облонских.")
        with open(file2, 'w', encoding='utf-8') as f:
            f.write("А,Б,В,Г,Д")
        with open(file3, 'w', encoding='utf-8') as f:
            f.write("и, в, не, на, с")

        return temp_dir, file1, file2, file3

    def run_test_for_layout(self, layout):
        analyzer = KeyboardAnalyzer(layout)
        temp_dir, file1, file2, file3 = self.create_temp_files()
        analyzer.analyze_all_files = lambda common_chars=None: [
            analyzer.analyze_text(open(file1, encoding='utf-8').read(), 'Война и мир', common_chars),
            analyzer.analyze_text(open(file2, encoding='utf-8').read(), 'Сортировка букв', common_chars),
            analyzer.analyze_text(open(file3, encoding='utf-8').read(), 'Минимальные фразы', common_chars),
        ]

        results = analyzer.analyze_all_files()

        assert len(results) == 3
        assert results[0]['text_name'] == 'Война и мир'
        assert results[1]['text_name'] == 'Сортировка букв'
        assert results[2]['text_name'] == 'Минимальные фразы'
        assert all(result['characters_analyzed'] > 0 for result in results)
        assert all(result['total_presses'] > 0 for result in results)

        for result in results:
            assert 'alt_count' in result
            assert 'shift_count' in result
            assert 'average_path' in result
            assert 'average_presses_per_char' in result

            assert isinstance(result['alt_count'], int)
            assert isinstance(result['shift_count'], int)
            assert isinstance(result['average_path'], float)
            assert isinstance(result['average_presses_per_char'], float)

            assert result['average_path'] > 0
            assert result['average_presses_per_char'] > 0

        temp_dir.cleanup()

    def test_ytsuken(self):
        self.run_test_for_layout('ytsuken')

    def test_vyzov(self):
        self.run_test_for_layout('vyzov')

    def test_zubachev(self):
        self.run_test_for_layout('zubachev')

    def test_skoropis(self):
        self.run_test_for_layout('skoropis')

    def test_rusfon(self):
        self.run_test_for_layout('rusfon')

    def test_diktor(self):
        self.run_test_for_layout('diktor')

    def test_ant(self):
        self.run_test_for_layout('ant')
class TestPrintImprovedResultsAllLayouts:
    def create_fake_result(self, layout):
        return {
            'text_name': 'Тестовый текст',
            'layout': layout,
            'finger_counts': {
                'left_pinky': 10, 'left_ring': 20, 'left_middle': 30, 'left_index': 40,
                'right_index': 50, 'right_middle': 60, 'right_ring': 70, 'right_pinky': 80,
                'left_thumb': 15, 'right_thumb': 25
            },
            'left_hand_percentage': 55.5,
            'right_hand_percentage': 44.5
        }

    def run_layout_test(self, layout, capsys):
        analyzer = KeyboardAnalyzer(layout)
        fake_result = self.create_fake_result(layout)
        analyzer.print_improved_results([fake_result])

        output = capsys.readouterr().out
        assert f"УЛУЧШЕННЫЙ АНАЛИЗ: {fake_result['text_name']}" in output
        assert f"РАСКЛАДКА: {layout}" in output
        assert "НАГРУЗКА ПО ПАЛЬЦАМ (%)" in output
        assert "Пробел (правый большой):" in output
        assert "Alt/Shift (левый большой):" in output
        assert f"Соотношение рук: Левая {fake_result['left_hand_percentage']:.1f}% / Правая {fake_result['right_hand_percentage']:.1f}%" in output

    def test_ytsuken(self, capsys):
        self.run_layout_test('ytsuken', capsys)

    def test_vyzov(self, capsys):
        self.run_layout_test('vyzov', capsys)

    def test_zubachev(self, capsys):
        self.run_layout_test('zubachev', capsys)

    def test_skoropis(self, capsys):
        self.run_layout_test('skoropis', capsys)

    def test_rusfon(self, capsys):
        self.run_layout_test('rusfon', capsys)

    def test_diktor(self, capsys):
        self.run_layout_test('diktor', capsys)

    def test_ant(self, capsys):
        self.run_layout_test('ant', capsys)
class TestCompareLayoutsCommonChars:
    def test_compare_layouts_output(self, capsys):
        # Подготовка общих символов
        common_chars = get_common_chars()
        assert isinstance(common_chars, set)
        assert 'а' in common_chars
        assert '!' in common_chars
        assert ' ' in common_chars

        # Список раскладок
        layouts = [
            ('ytsuken', 'СТАНДАРТНАЯ'),
            ('vyzov', 'ВЫЗОВ'), 
            ('zubachev', 'ЗУБАЧЕВ'),
            ('skoropis', 'СКОРОПИСЬ'),
            ('rusfon', 'РУСФОН'),
            ('diktor', 'ДИКТОР'),
            ('ant', 'АНТ')
        ]

        # Фиктивный результат анализа
        fake_result = {
            'text_name': 'Тест',
            'layout': 'fake',
            'characters_analyzed': 100,
            'total_presses': 120,
            'average_presses_per_char': 1.2,
            'total_path': 250.0,
            'average_path': 2.5,
            'left_hand_only_percentage': 40.0,
            'right_hand_only_percentage': 30.0,
            'two_handed_percentage': 30.0,
            'shift_count': 10,
            'alt_count': 5,
            'finger_counts': {
                'left_pinky': 5, 'left_ring': 5, 'left_middle': 5, 'left_index': 5,
                'right_index': 5, 'right_middle': 5, 'right_ring': 5, 'right_pinky': 5,
                'left_thumb': 5, 'right_thumb': 5
            }
        }

        # Подменяем методы
        for layout_code, _ in layouts:
            analyzer = KeyboardAnalyzer(layout_code)
            analyzer.analyze_all_files = lambda chars: [fake_result] * 3
            analyzer.print_improved_results = lambda results: print(f"Печать для {layout_code}")

            # Вызываем вручную, как в оригинальной функции
            results = analyzer.analyze_all_files(common_chars)
            analyzer.print_improved_results(results)

        # Перехватываем вывод
        output = capsys.readouterr().out

        # Проверяем ключевые фрагменты
        assert "Печать для ytsuken" in output
        assert "Печать для vyzov" in output
        assert "Печать для zubachev" in output
        assert "Печать для skoropis" in output
        assert "Печать для rusfon" in output
        assert "Печать для diktor" in output
        assert "Печать для ant" in output

if __name__ == "__main__":
    import pytest
    pytest.main()






